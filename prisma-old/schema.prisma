generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/@prisma-old/client"
}

datasource dbMa {
  provider = "mysql" // or mysql, sqlite, etc.
  url      = env("MARIA_DATABASE_URL")
}

// The schema will be populated in the next step