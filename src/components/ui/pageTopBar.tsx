import React from 'react'
import Typography from './typography'

type Props = {}

const PageTopBar = (props: Props) => {
   return (
      <div className="w-full mb-4 flex flex-row justify-between border-b border-gray-700 ">
      </div>
   )
}

export default PageTopBar